# 机器人行业商务英语学习材料统一模板

以后每次整理产品介绍，都按以下结构制作：

1. 完整英文音频，并提供完整英文原文和中文翻译。
2. 完整音频跟随模式：播放时当前英文句和中文句自动高亮。
3. 逐句慢速跟读音频，英文原句，中文翻译，互动语法教练和辅助读音。
4. 重点单词：英文、词性、中文含义、音标和可点击发音按钮。
5. 技术词组和常用搭配：说明在机器人行业中的实际用法。
6. 客户价值表达：把技术参数转换成客户能理解的价值。
7. 客户常见问题与回答：包括参数、应用、集成、交期和售后场景。
8. 30秒、90秒和3分钟版本，分别用于展会、电梯交流和正式会议。
9. 适合商务沟通的句型：确认需求、澄清问题、表达能力、报价跟进和会议总结。
10. 中文翻译默认折叠，需要时点击展开；完整中文翻译只使用一个折叠。逐句语块中文用一个开关显示／隐藏，整句中文单独折叠。整块语法讲解也默认折叠，标题保留主题和沟通目的，展开后再展示语块、一条规则和深入练习；从复习入口进入时自动展开对应讲解。
11. 发音细节包括音标、重音、连读、词尾辅音、易错音和分组跟读提示。
12. 页面保持统一的模块导航和视觉层级，方便每天追加新的产品模块并长期复习。
13. 页面按手机优先设计：音频宽度自适应、按钮适合触摸、表格可以横向滑动、导航可以左右滑动。
14. 所有音频使用相对路径；交付时同时提供可整体复制或压缩传输到手机的文件包。
15. 全文、逐句、商务表达、客户问答及单词音频统一支持播放／暂停／从暂停位置继续，并提供独立的“从头播放”和“循环播放”。暂停与续播不能重设音频地址或归零；循环只重复当前音频，切换到其他音频时暂停前一个，保留其进度。单词小喇叭重复点击也能暂停／继续，表格下显示当前单词的播放器和循环开关。
16. “今日练习”是独立入口，不塞进产品课程正文。资料库首页单独展示今日练习卡片；今日页集中放学习进度、主题选择和一次只练一个句子的练习区。产品课程页专注完整音频、逐句精读、产品表达和客户资料。

学习页面中的词性缩写：

- `n.`：名词
- `v.`：动词
- `adj.`：形容词
- `adv.`：副词
- `prep.`：介词或介词短语

每次新增内容都保留统一的文件结构和音频命名方式，便于持续积累和复习。

## 互动语法教练（每日模块沿用）

每句按四步组织，先解决说话任务，再解释术语：

1. **把句子读懂**：一个客户沟通目的；按英文顺序拆分语块，标明“谁／做什么／对象／补充”；中文可隐藏；只突出一条当前最重要的规则。
2. **为什么这样写**：用3—4步从简单句搭到完整句；每步解释新增信息和中英文顺序；说明词形变化及常见混淆。术语配白话解释，不把“谓语、定语”等当作用户已经掌握的知识。
3. **我来组一句**：两题选择练习，每个选项都有针对性反馈；一个替换产品或参数的输出题；参考答案默认隐藏，配自查点、可保存草稿及“还需提示／能独立说”的自评。假设参数必须明确标记，不混入产品事实。自由表达不做简单字符串匹配评分。
4. **放进客户对话**：提问与示范回答各配一个可点击的本地音频，中文默认折叠；先听提问、自己回答，再展开示范。合成语音标明用途，不伪装真人录音。

今日练习入口按商务用途归类：介绍参数、说明能力、比较特点、描述运行、研发经历、技术结果、取得成果。显示答题数、最近答案正确数、日期和口语自评；优先复习错题，再做未答题。一次只呈现一个句子，避免在产品课程里出现很长的复习墙。展开讲解不算掌握；选择题正确也不等于能口头表达。

内容与交互分离：`learning-assets/grammar-lessons.js` 放本模块数据，`grammar-coach.js` 和 `grammar-coach.css` 供模块复用。`audio-controls.js` 和 `audio-controls.css` 统一提供暂停续播、循环及音频互斥播放，在语法界面生成后加载。新模块必须用独立且稳定的 moduleId；同一模块非破坏性更新不要随意改版本号或题目顺序，避免丢失或错配记录。后续跨模块复习需要显式汇总模块索引，当前页面只统计本模块。

当前练习记录仅存当前浏览器，不跨设备同步；浏览器禁止存储时应提示但不阻断练习。更新交付时同步网站目录、手机目录与两个压缩包。音频、样式、脚本均使用相对路径，不依赖第三方在线服务。
